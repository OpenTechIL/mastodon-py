"""ActivityPub federation primitives.

This package collects the low-level pieces every federated request
needs — HTTP signatures, JSON-LD canonicalization, key resolution —
before they're stitched together by the higher-level services in
phase 6.
"""
